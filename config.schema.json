{
  "pluginAlias": "FanjuFJW4",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "FanJu FJW4 Wi-Fi Weather Station plug-in for [Homebridge](https://github.com/nfarina/homebridge)",
  "footerDisplay": "If you have any suggestions, please open an issue on [GitHub](https://github.com/slavvka/homebridge-fanju-fjw4/issues).",
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "required": true,
        "default": "FanJuFJW4",
        "description": "You shouldn't need to change this."
      },
      "options": {
        "title": "",
        "type": "object",
        "required": true,
        "properties": {
          "username": {
            "title": "Username",
            "type": "string",
            "required": true,
            "description": "The username used for authentication in the WeatherSense app",
            "placeholder": "WeatherSense app username"
          },
          "password": {
            "title": "Password",
            "type": "string",
            "required": true,
            "description": "The password used for authentication in the WeatherSense app",
            "placeholder": "WeatherSense app password"
          },
          "pollingInterval": {
            "title": "Polling Interval",
            "type": "number",
            "description": "The frequency in seconds that the plugin polls the cloud to get device updates",
            "required": false,
            "minimum": 600
          }
        }
      }
    }
  }
}
